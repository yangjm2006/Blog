## 香鼠看樱花 26.8.11

### 题目描述

香鼠在公园看樱花。

公园的地图可以抽象成一棵有 $n$ 个节点的树。树上的每条边都有一个正整数长度。每个节点都种有足够多的樱花树，我们只关心每个节点当前正在盛开的樱花树数量。

香鼠初始位于 $1$ 号节点，初始时所有樱花树都没有开花。

接下来香鼠在公园里逛了 $m$ 个单位时间。在每个单位时间内，恰好会发生下列三种事件之一：

- 某个节点 $p$ 有 $x$ 棵樱花树开花；
- 某个节点 $p$ 有 $x$ 棵正在盛开的樱花树落花；
- 香鼠从当前所在节点 $u$ 移动到与 $u$ 相邻的节点 $v$。

两个节点之间的距离定义为它们之间唯一简单路径上所有边的长度之和。一棵位于节点 $p$ 的盛开樱花树与香鼠之间的距离，定义为香鼠当前所在节点与 $p$ 之间的距离。同一节点的每棵盛开的樱花树均独立计算。

对于每个时刻，在该时刻的事件发生后，请回答以下两个问题：

1. 香鼠到距离最近的一棵盛开的樱花树的距离；
2. 香鼠到所有盛开的樱花树的距离之和。

若当前没有任何盛开的樱花树，则第一个答案规定为 $-1$，第二个答案为 $0$。

### 输入格式

第一行输入两个整数 $n,m$，分别表示节点数量和时间数。

接下来 $n-1$ 行，每行输入三个整数 $u,v,w$，表示节点 $u$ 与节点 $v$ 之间有一条长度为 $w$ 的边。

接下来 $m$ 行，每行描述一个事件。首先输入一个整数 $op$：

- 若 $op=1$，再输入两个整数 $p,x$，表示这一时刻节点 $p$ 有 $x$ 棵樱花树开花；
- 若 $op=2$，再输入两个整数 $p,x$，表示这一时刻节点 $p$ 有 $x$ 棵正在盛开的樱花树落花；
- 若 $op=3$，再输入一个整数 $v$，表示这一时刻香鼠从当前所在节点 $u$ 移动到节点 $v$。

### 输出格式

对于每个时刻输出一行两个整数。

第一个整数表示香鼠到距离最近的一棵盛开的樱花树的距离，第二个整数表示香鼠到所有盛开的樱花树的距离之和。

若当前没有任何盛开的樱花树，输出 `-1 0`。

### 数据范围

对于所有测试数据：

- $2\le n,m\le 100000$；
- $op\in\{1,2,3\}$；
- $1\le u,v,p\le n$；
- $1\le w\le 100$；
- $1\le x\le 10^6$；
- 对于 $op=2$，保证事件发生前节点 $p$ 正在盛开的樱花树数量不少于 $x$；
- 对于 $op=3$，保证香鼠当前所在节点 $u$ 与节点 $v$ 之间存在一条边。

保证所有需要输出的答案均在有符号 $64$ 位整数范围内。

### 样例输入

```text
5 7
1 2 3
1 3 2
3 4 4
3 5 1
1 4 2
3 3
1 2 1
3 1
2 4 1
3 2
2 4 1
```

### 样例输出

```text
12 24
8 16
3 19
5 15
2 10
0 8
0 0
```
